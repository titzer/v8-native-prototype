diff --git a/src/d8.cc b/src/d8.cc
index d1eb959..b340cb3 100644
--- a/src/d8.cc
+++ b/src/d8.cc
@@ -49,6 +49,8 @@
 #include "src/v8.h"
 #endif  // !V8_SHARED
 
+#include "src/wasm/wasm-js.h"
+
 #if !defined(_WIN32) && !defined(_WIN64)
 #include <unistd.h>  // NOLINT
 #else
@@ -1170,6 +1172,9 @@ Local<ObjectTemplate> Shell::CreateGlobalTemplate(Isolate* isolate) {
           .ToLocalChecked(),
       os_templ);
 
+  // Install WASM API.
+  WasmJs::Install(isolate, global_template);
+
   return global_template;
 }
 
